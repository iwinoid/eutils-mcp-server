# Build and tool output
dist/
coverage/
node_modules/
.npmcache/
.mcp-inspector/

# Verbatim NCBI captures. These are byte-faithful responses recorded from the
# live API and used as parser fixtures. Reformatting them would destroy the
# property that makes them useful: that they are exactly what NCBI returned.
test/fixtures/

# Generated threat model reports. They are produced by the analysis workflow and
# checked by an external verification pass that reads table structure. A
# formatter that rewraps a table can invalidate that check.
threat-model-*/

# Binary reference material
E-utilities.pdf

# Lockfile is generated by npm and must match its own formatting exactly.
package-lock.json
